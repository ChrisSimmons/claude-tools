# Prompts

MCP prompt templates exposed by this server.

Prompts are reusable, parameterized message templates that clients can request by name. Add a prompt by creating a file here and registering it in `../index.ts`.
