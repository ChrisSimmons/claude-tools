---
name: refresh-dotnet-rules
description: Reload local .NET-specific rules mid-session.
---

Read `~/.claude/rules/dotnet.md` and apply the rules for this session. Useful when discussing .NET conventions without having read a `.cs`, `.sln`, or `.csproj` file recently enough to trigger the rule automatically.
